rules_version = '2';
// ใช้เฉพาะ Firestore Emulator บนเครื่อง — เปิด read/write ทั้งหมด
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
